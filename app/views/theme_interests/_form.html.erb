<%= form_for(theme_interest) do |f| %>
  <% if theme_interest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(theme_interest.errors.count, "error") %> prohibited this theme_interest from being saved:</h2>

      <ul>
      <% theme_interest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
